package com.example.music_app.ui.player

/** Render model for one row in the full-player playlist picker dialog. */
data class PlaylistPickerItem(
    val id: String,
    val name: String,
    val subtitle: String
)
